// Static share page - prerender for static adapter compatibility
export const prerender = true;

// Enable client-side rendering for dynamic content
export const csr = true;

// Disable SSR since we need client-side routing for query parameters
export const ssr = false;