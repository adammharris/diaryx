// For static adapter: prerender the layout but let pages handle their own prerendering
export const prerender = true;

// Enable client-side rendering for dynamic functionality
export const csr = true;

// Disable SSR for static adapter compatibility with dynamic routes
export const ssr = false;