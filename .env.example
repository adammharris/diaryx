# API Configuration
# Copy this file to .env and fill in your API details

# Backend API URL (Vercel deployment)
VITE_API_BASE_URL=https://your-vercel-app.vercel.app

# Neon Database Configuration (for backend only - not exposed to client)
# These are used in Vercel serverless functions
# DATABASE_URL=postgresql://username:password@your-neon-endpoint/dbname

# Authentication Configuration
# Configure which authentication providers to enable
VITE_AUTH_PROVIDERS=google,github,email

# Development settings
VITE_APP_ENV=development

# Optional: Enable debug logging
# VITE_DEBUG_CRYPTO=true
# VITE_DEBUG_API=true